name: CI/CD Pipeline

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

env:
  PYTHON_VERSION: '3.10'

jobs:
  test:
    name: Run Tests
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: ${{ env.PYTHON_VERSION }}
        cache: 'pip'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install pytest pytest-cov black flake8
    
    - name: Run linting
      run: |
        black --check src/ flows/ deployments/
        flake8 src/ flows/ deployments/ --max-line-length=100
    
    - name: Run tests
      run: |
        pytest tests/ -v --cov=src --cov-report=xml
    
    - name: Upload coverage
      uses: codecov/codecov-action@v3
      with:
        file: ./coverage.xml
        fail_ci_if_error: false

  validate-flows:
    name: Validate Metaflow Flows
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: ${{ env.PYTHON_VERSION }}
    
    - name: Install Metaflow
      run: |
        python -m pip install --upgrade pip
        pip install metaflow
    
    - name: Validate flow syntax
      run: |
        for flow in flows/*.py; do
          echo "Validating $flow"
          python $flow show
        done

  deploy-staging:
    name: Deploy to Staging
    needs: [test, validate-flows]
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/develop'
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v4
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: us-west-2
    
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: ${{ env.PYTHON_VERSION }}
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install outerbounds

    - name: Deploy Scheduled Flow
      env:
        OUTERBOUNDS_API_KEY: ${{ secrets.OUTERBOUNDS_API_KEY }}
      run: |
        python flows/climate_data_refresh_flow.py --environment=staging create

    - name: Deploy API to Outerbounds Staging
      env:
        OUTERBOUNDS_API_KEY: ${{ secrets.OUTERBOUNDS_API_KEY }}
      run: |
        chmod +x deployments/climate-api/deploy.sh
        ./deployments/climate-api/deploy.sh staging

    - name: Deploy UI to Outerbounds Staging
      env:
        OUTERBOUNDS_API_KEY: ${{ secrets.OUTERBOUNDS_API_KEY }}
      run: |
        chmod +x deployments/climate-ui/deploy.sh
        ./deployments/climate-ui/deploy.sh staging

  deploy-production:
    name: Deploy to Production
    needs: [test, validate-flows]
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    environment: production
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v4
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: us-west-2
    
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: ${{ env.PYTHON_VERSION }}
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install outerbounds

    - name: Deploy Scheduled Flow
      env:
        OUTERBOUNDS_API_KEY: ${{ secrets.OUTERBOUNDS_API_KEY }}
      run: |
        python flows/climate_data_refresh_flow.py --environment=production create

    - name: Deploy API to Outerbounds Production
      env:
        OUTERBOUNDS_API_KEY: ${{ secrets.OUTERBOUNDS_API_KEY }}
      run: |
        chmod +x deployments/climate-api/deploy.sh
        ./deployments/climate-api/deploy.sh production

    - name: Deploy UI to Outerbounds Production
      env:
        OUTERBOUNDS_API_KEY: ${{ secrets.OUTERBOUNDS_API_KEY }}
      run: |
        chmod +x deployments/climate-ui/deploy.sh
        ./deployments/climate-ui/deploy.sh production

    - name: Create release
      uses: actions/create-release@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        tag_name: v${{ github.run_number }}
        release_name: Release v${{ github.run_number }}
        draft: false
        prerelease: false
