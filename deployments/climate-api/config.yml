name: climate-api
port: 8000
description: Climate prediction REST API

resources:
  cpu: "2"
  memory: "8Gi"
  gpu: "0"
  disk: "20Gi"
  shared_memory: "1Gi"

replicas:
  min: 1
  max: 1

commands:
  - uvicorn app:app --host 0.0.0.0 --port 8000
