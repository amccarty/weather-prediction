name: climate-ui
port: 8001
description: Climate prediction Streamlit dashboard

resources:
  cpu: "1"
  memory: "2Gi"
  gpu: "0"
  disk: "10Gi"
  shared_memory: "512Mi"

replicas:
  min: 1
  max: 1

commands:
  - streamlit run app.py --server.port 8001 --server.address 0.0.0.0
